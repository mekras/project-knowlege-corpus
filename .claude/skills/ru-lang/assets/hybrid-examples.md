# Примеры гибридных форм

Используй этот файл как набор образцов для однотипных замен. Он нужен, чтобы
узнавать форму ошибки, а не ограничиваться перечисленными словами.

## Имя технологии плюс русское родовое слово

- `PHP-решение` → `решение на PHP`
- `TypeScript-компонент` → `компонент TypeScript`
- `Docker-образ` → `образ Docker`
- `JSON-файл` → `файл JSON`
- `JSON-файлы` → `файлы JSON`
- `JSON‑файл` → `файл JSON`
- `JSON‑файлы` → `файлы JSON`
- `APM-скрипт` → `скрипт APM`
- `APM-доставляемая инструкция` → `инструкция, доставляемая через APM`
- `APM-зависимости` → `зависимости APM`
- `APM-зависимостей` → `зависимостей APM`
- `Claude-инструкция` → `инструкция Claude`
- `Claude-инструкции` → `инструкции Claude`
- `Inspect-модуль` → `модуль Inspect`
- `MCP-инструменты` → `инструменты MCP`
- `JSON-сценарии` → `сценарии JSON`
- `Python-валидаторы` → `валидаторы Python`
- `Keycloak-реализация` → `реализация через Keycloak`

## Рабочий термин на английском внутри русской фразы

- `auth-contract` → `контракт аутентификации`
- `token claims` → `утверждения токена`
- `dev-зависимость` → `зависимость для разработки`
- `lockfile-хеш` → `хеш файла блокировки`
- `sample-объект` → `объект-пример` или `пример объекта`
- `samples` → `примеры`
- `runner` → `средство запуска`
- `oracle` → `эталон`, `эталонный результат`, `эталонная проверка`
- `judge` → `проверка`, `сравнение`
- `trigger` → `срабатывание`, `условие срабатывания`
- `result` → `результат`
- `eval` → `проверка`, `оценка`
- `skill` → `навык`
- `staged` → `подготовленный к коммиту`, `в индексе Git`

## Отчёты агента о навыках и Git

- `APM-доставляемая инструкция` → `инструкция, доставляемая через APM`
- `Claude-инструкции` → `инструкции Claude`
- `Итоговый diff` → `итоговый набор изменений`
- `lockfile-хеш` → `хеш файла блокировки`
- `APM-зависимостей` → `зависимостей APM`
- `result-сценарий` → `сценарий результата`
- `result-сценарии` → `сценарии результата`
- `trigger-кейсы` → `случаи проверки срабатывания`
- `trigger-проверки` → `проверки срабатывания`
- `trigger evals` → `проверки срабатывания`
- `skill-каталог` → `каталог навыка`
- `skill-каталоги` → `каталоги навыков`
- `staged-файл` → `файл в индексе Git` или `подготовленный к коммиту файл`
- `staged-файлы` → `файлы в индексе Git` или `подготовленные к коммиту файлы`
- `JSON-файлы синтаксически корректны` → `файлы JSON прошли синтаксическую проверку`
- `JSON-файлы синтаксически корректны` → `файлы JSON без синтаксических ошибок`
- `skill-каталоги распознаны корректно` → `каталоги навыков распознаны как каталоги навыков`
- `skill-каталоги распознаны корректно` → `каталоги навыков определены как каталоги навыков`

## Английские режимы доступа

- `read-only режим` → `режим только для чтения`
- `bounded read-only` → `ограниченный режим только для чтения`
- `bounded read-only` → `ограниченный запуск без записи`
