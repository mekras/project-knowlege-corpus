interface:
  display_name: "statement-extraction"
  short_description: "Извлечение проверяемых утверждений из данных корпуса"
  default_prompt: "Используй $statement-extraction, чтобы извлечь проверяемые утверждения из первичных или нормализованных данных."

policy:
  allow_implicit_invocation: true
