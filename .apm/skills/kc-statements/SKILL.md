---
name: kc-statements
description: >
  Используй, когда нужно извлечь проверяемые утверждения, факты и наблюдения из
  первичных или нормализованных данных для дальнейшей проектной работы.
---

# Навык: извлечение утверждений из данных

## Назначение

Этот навык задаёт порядок извлечения из первичных данных и их нормализованных
представлений проверяемых утверждений, пригодных для последующего
проектирования.

Навык нужен для того, чтобы переход от данных к содержательным выводам
происходил последовательно, с сохранением связи с источником и с явным
разделением фактов, наблюдений и выводов.

`kc-statements` — канонический навык извлечения проверяемых единиц
корпуса. Он покрывает и конечные утверждения, на которые могут ссылаться
документы проекта, и промежуточные кандидаты для последующего
`kc-impact-audit`.

## Когда применять навык

Навык применяется, если нужно:

- выделить из материала значимые утверждения;
- подготовить основу для требований, правил, решений или документации;
- зафиксировать содержательные единицы, которые можно проверять и сопоставлять;
- отделить подтверждаемое источником содержание от интерпретации.

Если пользователь явно просит заполнить или проверить промежуточную таблицу
кандидатов для `kc-impact-audit`, применяй этот навык, но используй
локальные ID промежуточного артефакта, а не конечный ссылочный формат
`<SOURCE-ID>-NNN`.

## Входные данные

Для применения навыка должны быть доступны:

- первичный материал или его нормализованное представление;
- сведения о происхождении источника;
- известные ограничения источника;
- понимание, для какого следующего шага извлекаются утверждения.

Если этого недостаточно, ограничение должно быть зафиксировано явно.

В составе `kc-pipeline` конечные утверждения извлекаются только из готового
нормализованного материала. Если нормализованный слой источника имеет статус
"ожидает нормализации", перечисляет ожидаемые страницы или не содержит
нормализованных файлов для чтения, нужно остановиться и вернуть источник на
стадию `kc-normalization`. Не создавай проектный конечный артефакт утверждений
только для фиксации отсутствия нормализации.

Для локальных книг и длинных документов конечные утверждения можно извлекать
только из проверенного рабочего материала: нормализованного фрагмента,
разрешённой выдержки, локальной текстовой выгрузки с зафиксированным хэшем и
страницами или другого артефакта, который прослеживается к первичному файлу.
Технический паспорт книги, индекс страниц, наличие локального файла или
сообщение «текст можно извлечь позже» не являются основанием для утверждений.

## Порядок применения

### 1. Определить цель извлечения

Нужно зафиксировать:

- из какого материала извлекаются утверждения;
- для какого следующего шага это делается;
- какие типы утверждений нужны;
- какие выводы ещё нельзя делать на этом этапе.

### 2. Проверить основание

До извлечения должно быть понятно:

- на каком материале строится работа;
- является ли материал первичным или нормализованным;
- какие ограничения этого материала уже известны;
- можно ли проследить каждое утверждение до основания.

Если основание неясно, извлечение выполнено с нарушением навыка.

Если работа идёт в конвейере первичных данных, основание считается неясным,
когда доступен только паспорт источника, первичный паспорт или корневой файл
нормализованного слоя без готового нормализованного материала.
Для локальной книги основание также считается неясным, если известны только
хэш, формат, размер, число страниц или путь к локальному файлу без проверенного
текста выбранного фрагмента.

### 3. Выделить утверждения

Нужно извлекать утверждения так, чтобы каждое из них:

- относилось к конкретному фрагменту материала;
- было сформулировано проверяемо;
- не смешивало несколько разных смыслов без необходимости;
- не включало лишнюю интерпретацию.

Извлекай не только факты и выводы, но и виды содержания, которые легко
потерять: определения и термины, паттерны и антипаттерны, процедуры и приёмы,
критерии и правила, движущие силы и факторы выбора, конкретные примеры, образцы
и шаблоны. Непереносимость примера — это его ограничение, а не основание не
извлекать. Перечень видов см. в `references/extraction-coverage.md`.

### 3a. Обеспечить полноту охвата

Для содержательного источника (книга, длинный документ, стандарт, набор
записей) извлечение должно быть сплошным по его собственной структуре, а не
выборочным. Перечисли структурные единицы источника (оглавление, главы,
разделы, типы записей) и веди карту охвата: каждая единица получает статус
`извлечено`, `нет значимого содержания` или `отложено` с причиной. Структурная
единица без статуса и «молчаливый пропуск» запрещены. Порядок и форму карты
охвата см. в `references/extraction-coverage.md`.

Если у источника есть `source-map.yml`, используй его `structure.units` как
единственный канонический список структурных единиц. Не строй несовместимую
карту заново в заметках, ответе агента или отдельном ad hoc-файле. По мере
извлечения обновляй в `source-map.yml` раздел `coverage.units`: связывай
`unit_id` со статусом, tracked-артефактами и `statements.yml`.

### 4. Отделить обработку от оценки сведений

Для каждого утверждения определи `kind` — вид утверждения:

- `fact` — факт, прямо утверждаемый источником;
- `observation` — наблюдение составителя о материале;
- `inference` — интерпретация или вывод из источника;
- `limitation` — оговорка, граница применимости, лицензии, доступа или
  публикации.

В договоре утверждений версии 2 не используй общий `status`. Он смешивает
завершённость обработки, качество основания и необходимость решения человека.
Записывай эти оси отдельно:

- `processing_status` показывает завершение извлечения, проверки
  прослеживаемости, обычной и усиленной смысловой проверки, а также
  сопоставления источников.
- `source_role` отделяет первичный, вторичный, пользовательский и неизвестный
  источник.
- `evidence_strength` оценивает силу основания независимо от роли источника.
- `confidence` означает уверенность в точности извлечения, а не истинность
  утверждения.
- `temporal_status` показывает актуальность во времени.
- `corroboration` фиксирует единственный источник, независимое подтверждение,
  противоречие или неприменимость сверки.
- `limitations` перечисляет конкретные ограничения применения.

Слабое, старое или единственное основание не делает утверждение
необработанным. Если извлечение и проверки завершены, оставь
`processing_status` завершённым и сохрани ограничения в оценочных полях.
Текущая локальная доступность источника не меняет `evidence_strength`.
Повторная сверка или повторное открытие того же первоисточника не считается
независимым `corroboration`. Дата технической попытки доступа не определяет
`temporal_status`.

Различай границы утверждений. «В сохранённом сообщении сказано X» можно
проверить относительно снимка. «X — действующая норма» требует отдельной
оценки актуальности и применимости, даже если текст снимка совпал с
первоисточником.
Тематика материала сама по себе не создаёт блокер и не требует отдельного
параметра. Передавай решение человеку только когда названо действие, которое
модель не вправе выполнить самостоятельно.

Обычную смысловую проверку выполняет стандартный класс исполнения. При низкой
уверенности извлечения, противоречии или обнаруженной неоднозначности выставляй
`processing_status.strong_review: pending` и передавай утверждение сильному
классу независимой проверки. Конкретную модель определяет локальная оснастка.
Значение `blocked` допустимо только после усиленной проверки, когда безопасное
умолчание, явная фиксация неизвестного и обратимый вариант не закрыли случай, а
остаётся конкретное решение вне полномочий модели.

Файлы без `statement_contract_version` остаются договором версии 1 на время
миграции. Их `candidate` означает очередь переноса и автоматической проверки,
а не массовую очередь ручного подтверждения.

### 5. Назначить идентификатор утверждения

Для каждого утверждения нужно назначать устойчивый идентификатор, пригодный для
ссылок из анализа, отчётов о влиянии и производных документов.

До нумерации должен быть определён короткий идентификатор источника, а формат
ID должен соответствовать правилам из `references/statement-ids.md`.

### 6. Зафиксировать ограничения

Нужно отдельно отмечать:

- неполное основание;
- неоднозначную формулировку;
- зависимость от качества нормализации;
- сомнительную интерпретацию;
- необходимость проверки человеком.

### 7. Отделить извлечение от следующего этапа

Извлечение утверждений не должно включать:

- формулирование требований;
- выбор проектного решения;
- введение нового правила;
- объединение утверждений в окончательный документ;
- утверждение спорного вывода как принятого.

Если это уже требуется, работа должна переходить к следующей роли или следующему
навыку.

### 8. Зафиксировать результат

По итогам должно быть понятно:

- какие утверждения извлечены;
- каковы их вид, состояние обработки и отдельные оценки сведений.
- на каком основании они извлечены;
- какие структурные единицы источника охвачены, а какие отложены и почему;
- какие ограничения нужно учитывать;
- для какого следующего шага этот набор пригоден.

Для длинного источника результат должен также обновить `source-map.yml`, а не
оставлять карту охвата только в тексте ответа агента.

## Что читать дополнительно

- Для правил назначения идентификаторов утверждений и различия между конечными
  и промежуточными артефактами см. `references/statement-ids.md`.
- Для полноты охвата содержательного источника, видов извлекаемого содержания и
  формы карты охвата см. `references/extraction-coverage.md`.

## Результат применения навыка

Результатом должен быть набор утверждений, который:

- связан с конкретным материалом;
- разделён по видам, состоянию обработки и оценкам сведений.
- не подменяет данные проектными выводами;
- пригоден для следующего этапа обработки.

## Что считается ошибкой применения навыка

- утверждение нельзя проследить до источника;
- конечное утверждение извлечено в конвейере из источника, который ещё ожидает
  нормализации;
- конечное утверждение извлечено из локальной книги по техническим метаданным,
  индексу страниц или непроверенной OCR-выгрузке;
- вывод выдан как факт;
- договор версии 2 использует общий `status` вместо раздельных полей.
- спорный фрагмент подан как однозначный;
- извлечение незаметно перешло в требования или решения;
- ограничения источника не отражены в результате;
- утверждения настолько расплывчаты, что их нельзя проверить;
- извлечение из содержательного источника объявлено завершённым без карты
  охвата или со структурными единицами без статуса;
- карта охвата для длинного источника построена отдельно от `source-map.yml` и
  не синхронизирована с его структурой;
- значимое содержание (паттерны, силы, примеры) молча пропущено или отброшено
  как непереносимое без статуса `отложено` с причиной.

## Связь с другими элементами продукта

Этот навык должен применяться вместе:

- с принципом корпуса первичных данных;
- с навыком приёма первичных данных;
- с навыком нормализации первичных данных;
- с ролью `kc-inventory`;
- с ролью нормализатора данных;
- с ролью извлекателя утверждений;
- с контрольным слоем, если результат извлечения влияет на значимые решения
  проекта.
